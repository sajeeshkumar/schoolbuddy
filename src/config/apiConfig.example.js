/**
 * API Configuration Template
 * 
 * SETUP: Copy this file to apiConfig.js in the same directory:
 *   cp apiConfig.example.js apiConfig.js
 * 
 * Then replace the placeholder with your real Gemini API key.
 * Get a free key at: https://aistudio.google.com/apikey
 * 
 * IMPORTANT: apiConfig.js is gitignored — never commit your real key!
 */
export const API_CONFIG = {
    GEMINI_API_KEY: 'YOUR_GEMINI_API_KEY_HERE',
};
